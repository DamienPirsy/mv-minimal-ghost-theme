{{#if primary_tag}}
    <aside class="mb-2">
        {{#get "posts" filter="tags:{{primary_tag.slug}}+id:-{{id}}" limit="3" as |related_posts|}}
            {{#if related_posts}}
                <h4 class="title is-5 has-background-link-light">Vedi anche:</h4>
                <ul>
                    {{#foreach related_posts}}
                    <li>
                        {{>"post-element"}}
                    </li>
                    {{/foreach}}
                </ul>
            {{/if}}
        {{/get}}
    </aside>
{{/if}}